[user]
    name = Nelli Skogman
    email = nelli.skogman@gmail.com

[core]
    hooksPath = ~/.local/share/git-hooks

[pull]
    rebase = true

[push]
    autoSetupRemote = true

[init]
    defaultBranch = main

[alias]
    graph = log --graph --date=relative --pretty=tformat:'%C(green)%s%C(reset) %C(magenta)%an%C(reset) %C(red)%ad%C(reset) %C(yellow)%d%C(reset)'
    undo = reset --soft HEAD^
